const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.goto('https://klipy.com/gifs/atomic-bomb-explosion-6'); const imgUrl = await page.evaluate(() => { const img = document.querySelector('img.gif-image, video source, img'); return img ? (img.src || img.getAttribute('src')) : null; }); console.log(imgUrl); await browser.close(); })();
