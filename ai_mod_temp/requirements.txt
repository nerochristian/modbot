# AI Moderation Cog — Dependencies

# Core Discord.py (2.4+ required for LayoutView support)
discord.py>=2.4.0

# Pydantic for AI response validation
pydantic>=2.0.0

# Async SQLite database
aiosqlite>=0.19.0

# HTTP client for AI API calls
aiohttp>=3.9.0

# Web search (optional — for research mode)
duckduckgo-search>=4.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# The following are expected to already be installed in your bot:
# - utils.cache (RateLimiter) — your bot's rate limiter
# - utils.deepseek_web (DeepSeekWebClient) — your bot's DeepSeek web client
# - utils.checks (is_bot_owner_id) — your bot's owner check
# - utils.embeds (compact_kv_lines) — your bot's embed helpers
# If your bot doesn't have these, see README.md for adapter instructions.
