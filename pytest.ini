[pytest]
# Only collect the real project suite; skip local scratch/artifact dirs
# (ai_mod_temp/, scratch/, backups/ are gitignored working copies).
testpaths = tests
norecursedirs = ai_mod_temp scratch backups refs gc dashboard .git __pycache__ .venv venv
