# This dashboard now loads environment variables from the project root ../.env
# via dotenv in next.config.ts + src/lib/env.ts.
# Do not add secrets here. All DISCORD_*, DATABASE_URL, AUTH_SECRET live in root .env
DATABASE_URL="file:./dev.db"
