discord.py>=2.7.0
python-dotenv>=1.0.0
# Was only present transitively via discord.py, while transport.py and the
# search harness both depend on it directly.
aiohttp>=3.9.0
# Article extraction for the search harness. Without it the harness falls back
# to a crude regex strip, which works but produces much noisier source text.
trafilatura>=1.12.0
aiosqlite>=0.19.0
asyncpg>=0.30.0
humanize>=4.8.0
PyNaCl>=1.5.0
Pillow>=10.0.0
gTTS>=2.4.0
pydantic>=2.0.0
static-ffmpeg>=2.5.0
ffmpeg-python>=0.2.0
discord-ext-voice-recv>=0.5.1a170
edge-tts>=6.1.0
elevenlabs>=1.0.0
redis>=5.0.0
pypika>=0.48.9
playwright>=1.40.0
imageio>=2.31.0
audioop-lts>=0.2.1; python_version >= "3.13"
websocket-client>=1.8.0
pytz>=2024.1
uvloop>=0.19.0; sys_platform != "win32"
